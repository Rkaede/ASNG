'use strict';

/** Class representing a ring */
class Ring {

    /**
     * Create a ring.
     * @param {number} length - Length of the ring
     */
    constructor(length) {
        this.length = length || 0;
    }

}
